/*
** EPITECH PROJECT, 2021
** clibrary
** File description:
** macro
*/

#pragma once

#define MAX(x, y) ((x) < (y) ? (y) : (x))
#define MIN(x, y) ((x) > (y) ? (y) : (x))
#define BUFFER_MIN ((size_t) 10)